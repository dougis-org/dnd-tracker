# =============================================================================
# DND TRACKER - ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# 
# This file contains all environment variables needed for the DND Tracker app.
# Copy this file to .env.local for local development and fill in the values.
# 
# SECURITY NOTE: Never commit actual environment variable values to version control!
# This file should only contain example/placeholder values.
# =============================================================================

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================

# NODE_ENV determines the runtime environment
# Options: development, production, test
# Default: development (for local dev)
NODE_ENV=development

# NEXT_PUBLIC_APP_URL is the base URL of your application
# Local development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags (development-only). Toggle landing page in non-production environments.
# Set to true to enable the local landing page at `/` when NODE_ENV !== 'production'.
NEXT_PUBLIC_FEATURE_LANDING=true

# =============================================================================
# CLERK AUTHENTICATION
# =============================================================================
# 
# Get these values from your Clerk Dashboard (https://dashboard.clerk.com)
# Create a new application or use an existing one.
# 

# Clerk publishable key (safe to expose to client-side)
# Example: pk_test_bGVnYWwtd2FzcC0xNi5jbGVyay5hY2NvdW50cy5kZXYk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Clerk secret key (keep this secure, server-side only)
# Example: sk_test_mxjr8MRAMhm5UybRYDSmxJ3wqbpBoRenwVBrplkDkl
CLERK_SECRET_KEY=

# Clerk webhook secret (for webhook endpoint security)
# Get this from Clerk Dashboard > Webhooks > Create webhook
# Example: whsec_1234567890abcdef
CLERK_WEBHOOK_SECRET=

# Clerk redirect URLs (customize for your app routing)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MongoDB connection string
# For MongoDB Atlas: mongodb+srv://[username]:[password]@[cluster].mongodb.net/[database]?retryWrites=true&w=majority
# For local MongoDB: mongodb://localhost:27017/dnd-tracker
# Example: mongodb+srv://user:password@cluster0.abc123.mongodb.net/dnd-tracker?retryWrites=true&w=majority
MONGODB_URI=

# Database name (if not specified in MONGODB_URI)
# Default: dnd-tracker
MONGODB_DB_NAME=dnd-tracker

# =============================================================================
# WEBHOOK CONFIGURATION (Feature 014 - MongoDB User Model & Webhook)
# =============================================================================

# Webhook secret for HMAC-SHA256 signature validation (optional)
# If set, incoming webhook requests MUST include a valid X-Webhook-Signature header.
# If unset, signature validation is disabled (but logged as WARN at startup).
# Generate: `openssl rand -hex 32`
# Example: 1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
WEBHOOK_SECRET=

# Maximum webhook payload size in bytes (default: 1MB = 1048576)
# Requests exceeding this size are rejected with HTTP 413 Payload Too Large
# Default: 1048576
WEBHOOK_MAX_PAYLOAD_SIZE=1048576

# Webhook processing timeout in milliseconds (default: 3000)
# Requests taking longer than this are rejected with HTTP 504 Gateway Timeout
# Default: 3000
WEBHOOK_TIMEOUT_MS=3000

# =============================================================================
# DEPLOYMENT & HOSTING
# =============================================================================

# Deployment platform (for platform-specific configurations)
# Options: vercel, fly, netlify, self-hosted
DEPLOYMENT_PLATFORM=fly

# Fly.io Deployment Configuration
# These are set automatically in Fly.io environment but can be overridden
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Application URLs
NEXT_PUBLIC_APP_URL=https://dnd-tracker-next-js.fly.dev

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Debug mode (for additional logging in development)
# Options: true, false
DEBUG=false

# Log level for application logs
# Options: error, warn, info, debug
LOG_LEVEL=info

# =============================================================================
# ENVIRONMENT VALIDATION
# =============================================================================
# 
# The following variables help validate that the environment is properly configured:
#

# Environment configuration version (increment when adding new required vars)
ENV_CONFIG_VERSION=1.0.0

# =============================================================================
# EXAMPLE VALUES FOR DIFFERENT ENVIRONMENTS
# =============================================================================
#
# LOCAL DEVELOPMENT:
#   NODE_ENV=development
#   NEXT_PUBLIC_APP_URL=http://localhost:3000
#   DEBUG=true
#   LOG_LEVEL=debug
#   DEPLOYMENT_PLATFORM=self-hosted
#
# STAGING:
#   NODE_ENV=production
#   NEXT_PUBLIC_APP_URL=https://staging.yourdomain.com
#   DEBUG=false
#   LOG_LEVEL=info
#   DEPLOYMENT_PLATFORM=fly
#
# PRODUCTION (Fly.io):
#   NODE_ENV=production
#   NEXT_PUBLIC_APP_URL=https://dnd-tracker-next-js.fly.dev
#   DEBUG=false
#   LOG_LEVEL=error
#   DEPLOYMENT_PLATFORM=fly
#   PORT=3000
#   HOSTNAME=0.0.0.0
#
# =============================================================================

