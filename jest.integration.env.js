// Set environment variables for integration tests
process.env.JEST_INTEGRATION = 'true';

// Integration tests may require larger timeouts or other env settings in the future.
// Add additional env vars here as needed.
