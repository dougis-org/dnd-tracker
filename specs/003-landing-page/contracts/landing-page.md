# Contracts — Landing Page

This feature is UI-only and does not introduce API contracts. The landing page uses mock data fixtures only.

If future work requires dynamic content (testimonials/pricing), create REST endpoints under `/api/v1/marketing/*` and add OpenAPI contracts here.
